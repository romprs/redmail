Name:           redmail
Version:        0.0.1
Release:        139%{?dist}
Summary:        Почтовый клиент (аналог Outlook) для RED OS

License:        Proprietary
URL:            https://github.com/romprs/redmail
Source0:        %{name}-%{version}.tar.gz
Source1:        redmail.desktop
Source2:        redmail-reminder.desktop

BuildArch:      x86_64
BuildRequires:  python3 >= 3.9
BuildRequires:  python3-pip
BuildRequires:  python3-devel
# libpff-python (импорт .pst) собирается из исходников при pip install —
# готовых wheel под RED OS нет. Без этих пакетов сборка .pst-импорта тихо
# отсутствует в собранном RPM (было так до Release 9 — обнаружено, когда
# импорт .pst не заработал через установленный пакет, хотя работал из
# исходников на этой же машине, где эти пакеты уже стояли вручную).
BuildRequires:  gcc
BuildRequires:  make
BuildRequires:  automake
BuildRequires:  libtool
BuildRequires:  autoconf
BuildRequires:  zlib-devel
BuildRequires:  bzip2-devel
BuildRequires:  openssl-devel
# requests-gssapi/gssapi (вход в Exchange по Kerberos/SSO без пароля) тоже
# собирают C-расширение при pip install — против системных заголовков
# Kerberos, которые есть на любой машине, введённой в домен AD/FreeIPA.
BuildRequires:  krb5-devel

Requires:       python3 >= 3.9
# Голосовой помощник — отдельный продукт и отдельный пакет; «рекомендуемая»
# зависимость: dnf ставит его вместе с почтой, если пакет доступен (лежит
# рядом или есть в репозитории), и не мешает установке, если его нет.
Recommends:     audioreferent

# Всё под /opt/redmail/venv — сторонние pip-пакеты (в основном PySide6),
# не наш код. rpm's brp-mangle-shebangs требует, чтобы каждый исполняемый
# файл с #!/usr/bin/env python имел явную версию (python3), и падает с
# ошибкой на файлах вроде PySide6/scripts/pyside_tool.py, которые мы не
# редактируем и которые не нужно запускать напрямую (они библиотечные,
# не наши точки входа — единственная реальная точка входа, venv/bin/redmail,
# уже правильно переписана вручную в %%install). Исключаем весь venv из
# этой проверки вместо того, чтобы точечно чинить вендоренные файлы.
%global __brp_mangle_shebangs_exclude_from ^/opt/redmail/venv/.*$

# Автосканер зависимостей rpm просматривает КАЖДЫЙ .so под venv, включая
# Qt-плагины, которые redmail не грузит (Qt SQL под Oracle/Firebird/Mimer
# и т.п.) — и на них ловятся жёсткие Requires: на клиентские библиотеки
# этих СУБД, которых на системе никогда не будет и не нужно. Всё под
# /opt/redmail/venv самодостаточно (все свои зависимости внутри того же
# пакета), поэтому автогенерацию зависимостей для него отключаем целиком.
%global __requires_exclude_from ^/opt/redmail/venv/.*$
%global __provides_exclude_from ^/opt/redmail/venv/.*$

%description
RedMail — почтовый клиент для Linux (RED OS 8), функциональный аналог
Microsoft Outlook: почта по IMAP/SMTP, локальные архивы писем (свой
формат — аналог .pst по назначению, не побайтово; импорт из .pst/mbox/
Maildir) и календарь (локальное хранение событий; приглашения, ответы и
переносы встреч — через iTIP-вложения .ics в обычной почте, RFC 5546).
Такой подход работает одинаково поверх MS Exchange, VK Mail и любого
другого IMAP/SMTP-сервера — то, что и нужно в закрытой корпоративной
сети без прямого выхода к серверу календаря. Опционально — двусторонняя
синхронизация с CalDAV-сервером (адрес указывается вручную в
Параметрах), если такой сервер в сети доступен.

Приложение ставится вместе с приватным виртуальным окружением Python
(в /opt/redmail/venv) со всеми зависимостями внутри — собирается один раз
на этапе сборки пакета. После установки RPM работает офлайн, ничего не
скачивает и не требует доступа в интернет ни при установке, ни при
запуске (интернет нужен только МАШИНЕ, на которой собирается сам RPM).

%prep
%setup -q

%build
# Настоящая сборка (venv + зависимости) — в %%install, а не здесь: venv
# нужно создавать сразу по финальному пути установки (/opt/redmail/venv),
# иначе шебанги в venv/bin/* и pyvenv.cfg останутся указывать на путь
# сборки, а не на путь после rpm -i, и всё сломается на целевой машине.

%install
mkdir -p %{buildroot}/opt/redmail
python3 -m venv %{buildroot}/opt/redmail/venv
%{buildroot}/opt/redmail/venv/bin/pip install --no-cache-dir --upgrade pip wheel
%{buildroot}/opt/redmail/venv/bin/pip install --no-cache-dir %{_builddir}/%{name}-%{version}

# pip пишет шебанги (bin/pip, bin/redmail, bin/pyside6-*, ...) и
# pyvenv.cfg (строка command=) с ТЕКУЩИМ путём venv — то есть с путём
# сборки (%{buildroot}/...), а не с финальным путём установки
# (/opt/redmail/venv). check-buildroot иначе обрывает сборку, найдя путь
# сборки внутри установленных файлов; замена ниже убирает этот префикс.
find %{buildroot}/opt/redmail/venv -type f \( -name "pyvenv.cfg" -o -path "*/bin/*" \) -exec \
    sed -i "s|%{buildroot}||g" {} \;

mkdir -p %{buildroot}%{_bindir}
ln -s /opt/redmail/venv/bin/redmail %{buildroot}%{_bindir}/redmail
ln -s /opt/redmail/venv/bin/redmail-reminder %{buildroot}%{_bindir}/redmail-reminder

install -D -m 644 %{SOURCE1} %{buildroot}%{_datadir}/applications/redmail.desktop
install -D -m 644 %{SOURCE2} %{buildroot}%{_datadir}/applications/redmail-reminder.desktop
# Напоминания должны работать и когда почта закрыта — резидент поднимается
# при входе пользователя в систему.
install -D -m 644 %{SOURCE2} %{buildroot}%{_sysconfdir}/xdg/autostart/redmail-reminder.desktop
# Каталог фирменных оформлений организаций (раскладывает администратор).
mkdir -p %{buildroot}%{_sysconfdir}/redmail/brands

%posttrans
if ! rpm -q audioreferent >/dev/null 2>&1; then
cat <<'EOF'

Голосовой помощник (audioreferent) не установлен — он рекомендуемый пакет и
ставится вместе с почтой, только если указан в той же команде или доступен в
репозитории:

    dnf install ./redmail-*.rpm ./audioreferent-*.rpm

EOF
fi

%files
/opt/redmail
%{_bindir}/redmail
%{_bindir}/redmail-reminder
%{_datadir}/applications/redmail.desktop
%{_datadir}/applications/redmail-reminder.desktop
%{_sysconfdir}/xdg/autostart/redmail-reminder.desktop
%dir %{_sysconfdir}/redmail
%dir %{_sysconfdir}/redmail/brands

%changelog
* Mon Sep 21 2026 redmail <redmail@example.com> - 0.0.1-139
- Exchange: пометки, удаление и отправка письма больше не подвисают на минуту и дольше. Синхронизация календаря занимала единственное соединение с сервером, и всё остальное ждало её; теперь соединений несколько
- Календарь Exchange запрашивает у сервера только нужные поля встреч (без тел и вложений) — части окна перестают упираться в таймаут; после трёх таймаутов подряд проход откладывается до следующей синхронизации, а не тянется 10–18 минут
- Повтор запроса после таймаута ограничен двумя минутами (в 137–138 было пять — один зависший запрос держал соединение надолго)
* Fri Sep 19 2026 redmail <redmail@example.com> - 0.0.1-138
- Поиск календарей CalDAV: свои календари больше не показываются общими (VK не отвечает на запрос принципала — теперь спрашиваем его напрямую), одноимённые различаются в списке
- Календарь коллеги в CalDAV: поле «Календарь коллеги» и кнопка поиска в окне календаря — расшаренный календарь у VK лежит под учётной записью владельца, в свой список он не попадает
* Fri Sep 19 2026 redmail <redmail@example.com> - 0.0.1-137
- Окно параметров снова открывается (в 136 падало при каждом открытии из-за новой группы настроек)
- Календарь Exchange: сервер просил подождать 80 секунд, а программа сдавалась на 60 — календарь не загружался вовсе. Теперь ждём дольше, а после первой просьбы подождать не дёргаем сервер остальными частями окна и оставляем уже полученные встречи
* Fri Sep 19 2026 redmail <redmail@example.com> - 0.0.1-136
- Напоминания о чужих встречах: способ, время и, если нужно, список авторов — «Параметры» → «Общие»; в напоминании называется организатор и календарь
- Строка поиска писем, контактов и адресной книги сама исправляет раскладку: «bdfyjd» → «иванов»
- В режиме плитки поиск идёт по отправителю и теме, а не только по теме
- Заставка появляется сразу после запуска: модули грузятся уже при ней (в журнал пишется, через сколько секунд она показалась)
- Окно напоминания показывает тему и место текстом, а не разметкой
* Fri Sep 18 2026 redmail <redmail@example.com> - 0.0.1-135
- Напоминания о встречах: значок в трее следит за календарём текущего дня и напоминает окном, голосом или тем и другим; окно умеет отложить на 5 минут и открыть календарь. Работает и при закрытой почте (ставится в автозапуск)
- У каждой встречи свой выбор напоминания — за сколько минут и каким способом
- Основной календарь: «Мои календари» → правая кнопка → «Сделать основным». Туда попадают новые встречи, в том числе продиктованные голосом; локальный календарь переименован в «Задачи» (личные записи без сервера)
- Вложение-письмо (.eml) открывается своим окном: заголовки, тело, свои вложения, исходный текст, сохранение
- Поиск календарей CalDAV больше не ходит по ссылкам на чужие серверы (учётные данные туда не уходят, и враждебный ответ не роняет поиск)
* Fri Sep 18 2026 redmail <redmail@example.com> - 0.0.1-134
- При выключенной галочке «хранить письма ящиков коллег локально» их письма больше не скачиваются телами в фоне и не попадают в автоархив (проверено на живом сервере Exchange)
* Fri Sep 18 2026 redmail <redmail@example.com> - 0.0.1-133
- Подписка на ящики коллег в Exchange: адреса указываются в учётной записи, ящик открывается вашей учётной записью по выданным правам (пароль владельца не нужен) и виден отдельной веткой «Ящик …»
- Хранить письма ящиков коллег локально или открывать с сервера — галочка в учётной записи; выключено по умолчанию, чтобы база и автоархив не росли за чужой счёт
- Поиск календарей CalDAV: в журнал пишутся принципал, все дома календарей и каждый ответ сервера; обходятся календари тех, кто назначил вас доверенным лицом; если сервер не показал ничего, в списке остаётся настроенный календарь
* Fri Sep 18 2026 redmail <redmail@example.com> - 0.0.1-132
- Календарь Exchange снова синхронизируется: время встреч переводилось с ошибкой, и пропускались ВСЕ встречи (в журнале за день 7354 «встреча пропущена»)
- Часть окна календаря Exchange без ответа («The request timed out») больше не отменяет всю синхронизацию — берутся остальные части
- В недельной сетке окно создания события открывается двойным щелчком по пустому месту, одиночный только выделяет день
* Fri Sep 18 2026 redmail <redmail@example.com> - 0.0.1-131
- Канал помощника: переход к разделу почты, календаря или контактов («открой календарь» голосом)
* Thu Sep 17 2026 redmail <redmail@example.com> - 0.0.1-130
- Свой список приветствий: текст и время, когда оно подходит («Параметры» → «Общие» → «Список приветствий…»)
- В окне письма приветствие меняется на другое из списка или убирается
- Сортировка писем по категории: пункт «Категория» в меню сортировки и щелчок по заголовку столбца; письма без категории — в конце, внутри категории новые сверху
- Канал голосового помощника: отмена встречи без окон (подтверждение и «этот день или серия» спрашивает помощник), окно изменения открывается на нужном дне серии
* Thu Sep 17 2026 redmail <redmail@example.com> - 0.0.1-129
- CalDAV: в журнал пишется строение объектов календаря сервера (серия, правило повтора, часовой пояс, отменённые и изменённые дни — без названий и участников) и причина пропуска непонятного объекта
* Thu Sep 17 2026 redmail <redmail@example.com> - 0.0.1-128
- CalDAV: серия встреч запрашивается целиком и раскрывается в программе — серию снова можно менять целиком (проверено на Radicale и SOGo: перенос и отмена дня, переименование серии, календарь участника)
- Повторы «по дням недели» раскрываются в местном времени — утренние встречи не уезжают на соседний день
* Thu Sep 17 2026 redmail <redmail@example.com> - 0.0.1-127
- Повторяющиеся встречи: при переносе и отмене — «только этот день» или «вся серия»; день серии уходит на сервер CalDAV (RECURRENCE-ID, EXDATE) и Exchange, участникам — приглашение или отмена на этот день
- Правка всей серии на CalDAV больше не стирает на сервере уже перенесённые дни
- Перенос одного показанного дня серии в своём календаре больше не сдвигает всю серию
* Thu Sep 17 2026 redmail <redmail@example.com> - 0.0.1-126
- «Справка → Расход памяти»: расклад по процессу, просмотру писем, куче, данным интерфейса и Python-объектам; кнопка «Освободить и пересчитать», копирование, запись в журнал
- После автоархива и загрузки тел писем свободная память возвращается системе
- Фотографии адресной книги хранятся уменьшенными копиями (до 128 точек): книга целиком живёт в памяти
* Thu Sep 17 2026 redmail <redmail@example.com> - 0.0.1-125
- «Отправленные» Exchange: получатели берутся из display_to, затем из заголовка «Кому» письма, затем из строки «Кому:» в тексте
- Оборванный на середине список писем папки больше не удаляет локальные письма: при большой разнице папка перечитывается
- Текст не в той раскладке: подсказка адресатов ищет в другой раскладке, кнопка «Раскладка» (Pause, Ctrl+Shift+K) переводит выделенное или последнее слово
* Thu Sep 17 2026 redmail <redmail@example.com> - 0.0.1-124
- Повторяющиеся встречи CalDAV (VK) больше не пропадают: экземпляры серии хранятся отдельно, учитываются перенесённые и отменённые дни (RECURRENCE-ID, EXDATE)
- Календарь Exchange загружается частями по 14 дней — сервер отказывал при больше ~1000 встреч в одном запросе; экземпляры серий Exchange не схлопываются
* Thu Sep 17 2026 redmail <redmail@example.com> - 0.0.1-123
- Исходный текст письма: кнопка над письмом, пункт меню и Ctrl+U — заголовки и тело оригинала с сервера или из архива, поиск, копирование, сохранение .eml
- Письма, созданные редактором при локали KOI8-R (charset=koi8-r в HTML), показывались кракозябрами — объявление кодировки исправляется при показе, отправке и выгрузке
* Thu Sep 17 2026 redmail <redmail@example.com> - 0.0.1-122
- Выгрузка переписки не обрывается на адресах вида «Иванов И.И.<ivanov@…>» (Exchange) и адресах с кириллицей; неразобранное письмо пропускается и попадает в итог
- Выгрузка администратором от root не трогает файл журнала сотрудника
* Thu Sep 17 2026 redmail <redmail@example.com> - 0.0.1-121
- Исправлен запуск программы: сборка 120 не открывалась из-за ошибки в сообщении о переносе профиля
* Thu Sep 17 2026 redmail <redmail@example.com> - 0.0.1-120
- Ответить, Ответить всем, Переслать и Открыть в окне — кнопками, как над списком писем, строкой над реквизитами письма
- Окно учётной записи не сжимается ниже содержимого, подписи полей выровнены во всех группах
- Голосовой помощник — на вкладке «Модули» рядом с категориями писем; отдельной вкладки «Помощник» больше нет
- Фирменные оформления организаций: цвета и шрифт программы, шрифт и цвет письма, колонтитул и стандартная подпись по шаблону; файлы в /etc/redmail/brands, редактор и выгрузка в «Параметрах»
- Перенос профиля на другой компьютер одним файлом .rmprofile: проверка контрольных сумм, загрузка при следующем запуске, прежние данные — в резервную копию
- Выгрузка всей переписки и архивов в mbox или EML; из программы или администратором из командной строки (--export-mail, --export-profile, --import-profile)
- Кто выгружает переписку — сотрудник или только администратор — задаётся в оформлении организации (data_export); действия пишутся в системный журнал
* Thu Sep 17 2026 redmail <redmail@example.com> - 0.0.1-119
- Сортировка, фильтры и группировка по теме — свои у каждой учётной записи и архива, сохраняются между запусками
- Окно учётной записи по кнопке «Настроить…»: подключение, удаление писем с сервера и замена доменов; вкладка «Почта» в «Параметрах» убрана
- «Добавить учётную запись» открывает окно записи, а не все параметры; новая запись сразу появляется в списке
- Ответить, Ответить всем, Переслать и Открыть в окне — плоскими пунктами в одну строку в шапке письма
* Thu Sep 17 2026 redmail <redmail@example.com> - 0.0.1-118
- Подключаемые модули: вкладка «Модули» в «Параметрах», включение и выключение без перезапуска
- Модуль «Категории писем»: уведомления и рассылки, обращения и запросы, реклама и свои категории; описание по адресатам и словам темы
- Категории определяются по признакам рассылок в письме и дообучаются на ручной разметке; всё хранится только локально
- Колонка «Категория» в списке писем, метка в плитках, фильтр по категории, выбор категории правой кнопкой по письму
* Wed Sep 16 2026 redmail <redmail@example.com> - 0.0.1-117
- Удаление писем с сервера после архивации — своя настройка у каждой учётной записи
- Замена домена получателей — свои правила у каждой учётной записи; общие правила прежних версий действуют для каждой, пока их не разнесут
- Письмо уходит из учётной записи, в которой его начали, даже если во время набора открыли папку другой записи
- Exchange: даты писем в местном времени, а не по Гринвичу; ранее загруженные письма переводятся один раз
- Приветствие первой строкой нового письма, ответа и пересылки по настройке: «Здравствуйте!» или по времени суток
- Цепочка писем в области просмотра и список цепочки — от нового к старому
- Голосовой помощник: выбор календаря встречи («календарь эксчейндж», «календарь два»), подсветка поля, о котором спрашивают
* Wed Sep 16 2026 redmail <redmail@example.com> - 0.0.1-116
- Календари: получение встреч с сервера больше не обрывается на ошибке отправки (VK отвечал 412, и ничего не приходило)
- Календари: на сервер уходят только встречи, изменённые здесь; удаления передаются на сервер; встречи, удалённые на сервере, убираются
- Календарь CalDAV берёт логин и пароль почты своего сервера, а не той учётной записи, чья папка открыта
- Календари синхронизируются сами: после подключения почты, по таймеру и после правки встречи; кнопка «Синхронизировать» с подписью
- Приглашения встреч календаря Exchange рассылает сам Exchange, без дублей
- Встречи из писем попадают в календарь без открытия письма, учитываются и файлы .ics во вложениях; отмену принимаем только от организатора
- Exchange: удаление и перенос писем пакетно; уже удалённые на сервере письма не дают ошибку «ErrorItemNotFound»
- Exchange: колонка «Кому» в Отправленных, счётчики непрочитанных, письма без MIME открываются
- Перенос и удаление ждут синхронизацию той же папки и привязаны к ящику, где их начали
- Del — в корзину, Ctrl+Del — безвозвратно
- Ответить, Ответить всем, Переслать — в шапке открытого письма
- Раздел «Контакты» открывается сразу, если книга не менялась
- Картинки в письмах Outlook и Exchange показываются, метки [cid:…] в тексте убраны
- В дереве папок учётная запись подписана по-человечески, а не внутренним ключом
* Mon Sep 15 2026 redmail <redmail@example.com> - 0.0.1-115
- Учётная запись, которой вернули галочку в «Параметрах», подключается сразу, без перезапуска программы
* Mon Sep 15 2026 redmail <redmail@example.com> - 0.0.1-114
- Учётная запись, потерянная прежней ошибкой сохранения, возвращается в список из старых настроек при первом запуске
- Правка своей учётной записи в «Параметрах» больше не оставляет в списке вторую запись со старым адресом
* Mon Sep 15 2026 redmail <redmail@example.com> - 0.0.1-113
- Синхронизация Exchange больше не тянет каждое письмо целиком: список писем папки берётся заголовками, почта наконец появляется в списке, процессор не загружен
- Учётная запись, выключенная галочкой, остаётся в списке «Учётные записи» — её можно включить обратно (раньше следующее сохранение стирало её насовсем)
- Пункт «Отключить ящик» снова запоминает отключение
- Синхронизация больше не крутится вхолостую: раунд докачки ставится, только если предыдущий что-то скачал (в журнале было 1260 проходов за 40 секунд при занятом процессоре)
- Адрес сервера Exchange можно вставлять ссылкой из браузера — из неё берётся имя узла
- Если Exchange просит подождать, клиент ждёт столько, сколько просит сервер (не дольше минуты), а не считает это ошибкой
* Tue Sep 15 2026 redmail <redmail@example.com> - 0.0.1-112
- Параметры открываются, когда текущая учётная запись — Exchange (окно вообще не открывалось)
- Exchange: синхронизируются только почтовые папки, служебные (Задачи, Заметки, Файлы, SearchLog, Yammer и прочие) пропускаются — вместо 64 папок около десятка
- Если сервер просит подождать («The server cannot service this request right now»), круг по папкам прекращается до следующего раза, а не повторяет ошибку для каждой папки
- Календарь Exchange заводится сам при подключении учётной записи Exchange — отдельно добавлять его не нужно
* Tue Sep 15 2026 redmail <redmail@example.com> - 0.0.1-111
- Календарь Exchange по EWS: встречи читаются и отправляются тем же подключением, что и почта Exchange (источник «Exchange (EWS)» при создании календаря)
- Инструкция дополнена разделом про источники календаря
* Mon Sep 14 2026 redmail <redmail@example.com> - 0.0.1-110
- Надписи встреч в сетке календаря следуют масштабу шрифта, а не фиксированным 11 px
- Параметры → Общие: «Масштаб шрифта» (раньше только ползунок в строке состояния)
* Mon Sep 14 2026 redmail <redmail@example.com> - 0.0.1-109
- Окно письма: фотографии адресатов в списке под полем «Кому», выбор цвета текста
- Письмо и встреча: поля прижаты к верху, свободное место занимают список адресатов и текст (в встрече — участники и описание)
- Окно встречи стало обычным окном: полноразмерный заголовок, кнопки свернуть и развернуть
* Mon Sep 14 2026 redmail <redmail@example.com> - 0.0.1-108
- Адресная книга карточками: фотография сотрудника, имя, должность с подразделением, адрес и телефон; переключатель «таблица/карточки» рядом с поиском
* Mon Sep 14 2026 redmail <redmail@example.com> - 0.0.1-107
- Подпись: поле редактирования растягивается вместе с окном, добавлен выбор цвета текста
- Адресная книга: импортируются должность, подразделение и фотография сотрудника (в том числе ссылкой на файл); кривая карточка больше не обрывает импорт всей книги
- Контакты: аватар с фотографией, колонки «Должность» и «Подразделение», поиск по ним; фотография и должность в карточке контакта и в окне выбора адресатов
* Mon Sep 14 2026 redmail <redmail@example.com> - 0.0.1-106
- Кэш цепочки писем больше не держит вложения и встроенные картинки и ограничен 12 письмами — программа не растёт в памяти при чтении писем с вложениями
- Справка → «Расход памяти…»: сколько занимает программа и на что уходит память
* Mon Sep 14 2026 redmail <redmail@example.com> - 0.0.1-105
- Переезд между почтовыми системами: замена домена получателей при отправке (Параметры → Почта), письма по старым адресам уходят на новый сервер
- Один адрес в двух системах: учётные записи различаются по протоколу и серверу, Exchange добавляется рядом с VK
- Список учётных записей в настройках с выключателем: выключенная запись не подключается, настройки и локальная копия сохраняются
* Mon Sep 14 2026 redmail <redmail@example.com> - 0.0.1-104
- Параметры → Общие: «Корневой сертификат (PEM)» — файл корневого сертификата организации для CalDAV, Exchange и подписки на календарь; пусто — системное хранилище
* Mon Sep 14 2026 redmail <redmail@example.com> - 0.0.1-103
- Версия в заставке, «О программе» и журнале берётся у пакета, которому принадлежит запущенный файл: во время обновления в базе rpm недолго числятся обе версии, и показывалась старая
* Mon Sep 14 2026 redmail <redmail@example.com> - 0.0.1-102
- Неудачный SELECT больше не «теряет» папку: после сбоя сервера программа выбирает её заново (в журнале было 2000 ошибок «UID illegal in state AUTH» и стоящий автоархив)
- Ошибки [SERVERBUG] и потеря выбранной папки лечатся переподключением
- Автоархив останавливается после 20 неудач подряд, а не долбит сервер тысячами запросов
- HTTPS (CalDAV, Exchange, подписка на календарь) доверяет системному хранилищу сертификатов — корпоративный ЦС теперь известен
- Понятные подсказки к ответам серверов: пароль приложения, неизвестный сертификат, сбой сервера, автопоиск Exchange
- Список вложений по высоте содержимого, не постоянные 110 px
- После установки почты подсказка, как поставить голосовой помощник
* Sun Sep 13 2026 redmail <redmail@example.com> - 0.0.1-101
- Папка назначения досинхронизируется при каждом переносе: «Отправленные» после отправки (в фоне, без подвисания окна), «Черновики» после сохранения черновика, папки правил сортировки
* Sun Sep 13 2026 redmail <redmail@example.com> - 0.0.1-100
- Удалённые письма сразу видны в «Корзине»: папка назначения досинхронизируется после переноса
- Открытие любой папки тихо сверяется с сервером (раньше уже синхронизированная папка показывалась только из кэша до следующего полного прохода)
* Sun Sep 13 2026 redmail <redmail@example.com> - 0.0.1-99
- Адресаты с именами: список ФИО под полем, имена в подсказке и в карточке встречи (ветка claude/recipients-list)
- Канал управления: нечёткий поиск контактов (find_contacts)
* Sun Sep 13 2026 redmail <redmail@example.com> - 0.0.1-98
- Календарь: ширина боковой панели считается по мини-календарю при текущем шрифте и масштабе — левая колонка больше не обрезается
* Sun Sep 13 2026 redmail <redmail@example.com> - 0.0.1-97
- Параметры разнесены по вкладкам: Почта, Общие, Хранилище, Помощник, Учётные записи — окно помещается на экран
* Sun Sep 13 2026 redmail <redmail@example.com> - 0.0.1-96
- Крестик очистки в письме и во встрече — сразу за полем, с отступом до «Адресной книги»
* Sun Sep 13 2026 redmail <redmail@example.com> - 0.0.1-95
- Плитки: Ctrl-клик накапливает выделение и не сбрасывает набор
- Окно письма: кнопка очистки получателей стоит сразу за полем «Кому»
* Sun Sep 13 2026 redmail <redmail@example.com> - 0.0.1-94
- Поля «Кому», «Копия», «Скрытая копия» и «Участники» с крестиком очистки прямо в поле
- Письмо без получателя не отправляется и окно не закрывается (предупреждение); черновик сохраняется без получателей
* Sun Sep 13 2026 redmail <redmail@example.com> - 0.0.1-93
- Удаление и другие массовые действия работают по выделенным письмам без галочек, в том числе по одному открытому
- Плитки: множественное выделение, зеркальное таблице (Ctrl/Shift-клик)
* Sun Sep 13 2026 redmail <redmail@example.com> - 0.0.1-92
- Счётчики непрочитанных в дереве обновляются после удаления, переноса, отметки «прочитано» и обновления папки; опрос «Входящих» больше не сбрасывает числа у других папок
* Sun Sep 13 2026 redmail <redmail@example.com> - 0.0.1-91
- Канал управления: адресная книга на экране для формы встречи (contact_picker_open/select/state/accept/cancel) — помощник открывает книгу с фильтром, строки пронумерованы, выбор по номеру или имени, «принять» кладёт участников в форму
* Sun Sep 13 2026 redmail <redmail@example.com> - 0.0.1-90
- Группа в поле адресатов — короткой ссылкой «[Имя группы]», адреса подставляются при отправке; подсказка к полю показывает всех адресатов
- Окно «Адресная книга» отмечает уже выбранных, показывает число адресов; снятые галочки убирают адресата
- Диалог контакта: участников группы можно добавить из книги
- Раздел «Контакты»: поиск, фильтр «Все / Люди / Группы», значок группы, счётчик
* Sun Sep 13 2026 redmail <redmail@example.com> - 0.0.1-89
- Адресная книга: признак «Группа» (список адресов); у обычного контакта один адрес, запасные — в заметках
- Импорт: списки рассылки распознаются (участники «Имя <адрес>», имена в RFC 2047 декодируются); существующий адрес обновляется, а не дублируется
- В письме группа подставляется как «Имя: адреса;» и раскрывается в адреса участников; старые книги переразбираются автоматически
* Sun Sep 13 2026 redmail <redmail@example.com> - 0.0.1-88
- Отметка головного письма свёрнутой цепочки действует на всю цепочку (удаление группы удаляло одно письмо)
- Удалённые письма исчезают из списка сразу, сервер и обновление — в фоне
- Архивные письма при удалении/переносе трогаются и на сервере: иначе синхронизация возвращала их (корзина «наполнялась обратно»)
- Галочка на выделенной строке видна: квадрат цветом текста выделения, внутри отметка
* Sun Sep 13 2026 redmail <redmail@example.com> - 0.0.1-87
- Панель разделов: отступы сверху и снизу заданы компоновкой (QSS-отступ панель игнорировала)
* Sat Sep 12 2026 redmail <redmail@example.com> - 0.0.1-86
- Опрос раз в 5 минут всегда проверяет «Входящие» (и текущую папку); счётчики непрочитанных — по интерактивному соединению
* Sat Sep 12 2026 redmail <redmail@example.com> - 0.0.1-85
- Окно письма немодальное: главное окно не блокируется, окон писем может быть несколько, окно по каналу управления не вешает программу
- Действия пользователя (удалить, переместить, отметить, обновить папку, копия в «Отправленные») — по своему соединению, не ждут фоновую синхронизацию; удаление идёт в фоне
- Опрос каждые 5 минут — только текущая папка; полный проход по всем папкам раз в полчаса и только если предыдущий закончен
- Часы обслуживания базы (Параметры → Хранилище): автоархив, докачка писем и сжатие только в заданные часы
- Автоархив порциями по порогу (~500 МБ): база меньше порога — ничего не делаем, больше — переносим и сжимаем базу полностью
- Дерево папок: служебная «[Gmail]/Отправленные» и своя папка «Отправленные» — разные узлы (раньше открывалась пустая)
- Клик по заголовку колонки галочек отмечает все видимые письма — для удаления папки целиком
* Sat Sep 12 2026 redmail <redmail@example.com> - 0.0.1-84
- Панель разделов (Почта/Календарь/Контакты/Параметры/Справка): отступы сверху и снизу
* Sat Sep 12 2026 redmail <redmail@example.com> - 0.0.1-83
- Пересылка HTML-рассылок больше не вешает окно: в редактор идёт упрощённая разметка без вложенных таблиц и стилей; слишком тяжёлое письмо пересылается текстом
* Sat Sep 12 2026 redmail <redmail@example.com> - 0.0.1-82
- Подпись без названия не сохраняется: предупреждение вместо молчаливого закрытия; кнопки «Сохранить»/«Отмена»
- Кнопки архива над деревом папок того же размера, что и остальные панели
* Sat Sep 12 2026 redmail <redmail@example.com> - 0.0.1-81
- Письма без текстовой части: текст извлекается из HTML — в цепочке видно содержание, а не заглушка
- Пересылка: шапка «Пересланное сообщение» с явными цветами, видна и в тёмной теме
- Окно письма — обычное окно с полной рамкой; размер запоминается, по умолчанию 960×720
* Sat Sep 12 2026 redmail <redmail@example.com> - 0.0.1-80
- Цепочки по теме: письма одной темы стоят рядом под последним письмом при любой сортировке; сортировка действует на верхний уровень, внутри цепочки — по дате, новые выше
* Sat Sep 12 2026 redmail <redmail@example.com> - 0.0.1-79
- Закрытие окна больше не зависает на выходе с сервера: соединения закрываются в фоне не дольше 5 с, занятое соединение просто обрывается
* Sat Sep 12 2026 redmail <redmail@example.com> - 0.0.1-78
- Открытие письма идёт по отдельному соединению с сервером и не ждёт фоновую синхронизацию и автоархив
- Сортировка списка писем из меню «Сортировка» (работает и в режиме плиток); плитки следуют порядку таблицы
- Фильтр, сортировка и переключатель режима — в строке поиска, не прячутся при узком окне
- Тёмная тема: видимая полоса прокрутки; кнопки разделов без рамок; отступы в области чтения
- В цепочке писем предпросмотр HTML-писем вместо заглушки
- База ужимается по ходу автоархива и полностью при старте, если свободных страниц много
- Импорт контактов: имя собирается с фамилии (Фамилия Имя Отчество), колонка «Отчество» в CSV
- Адресная книга: кнопки со значком и подписью, как в почте
- Цепочки по теме в списке писем (таблица и плитки): видно последнее письмо, остальные раскрываются значком; отключается в меню «Сортировка»
* Sat Sep 12 2026 redmail <redmail@example.com> - 0.0.1-77
- Параметры → «Голосовой помощник»: включение/выключение сервиса audioreferent, состояние, его настройки и журнал, проверка связи; пакет audioreferent — рекомендуемая зависимость
* Sat Sep 12 2026 redmail <redmail@example.com> - 0.0.1-76
- Только один экземпляр программы: повторный запуск поднимает уже открытое окно
- Ужатие базы после автоархива порциями без замирания окна; первичное ужатие — при старте под заставкой
- Архивные письма больше не перекачиваются как новые при каждой синхронизации
* Sat Sep 12 2026 redmail <redmail@example.com> - 0.0.1-75
- Автоархив переносит самые старые письма из тех, чьи тела уже лежат в базе, — именно они занимают место
* Sat Sep 12 2026 redmail <redmail@example.com> - 0.0.1-74
- Файлы автоархива хранятся в профиле (<профиль>/archives); уже созданные переезжают туда автоматически
* Fri Sep 11 2026 redmail <redmail@example.com> - 0.0.1-73
- Автоархив: раунды по 300 писем с ужатием базы после каждого, один архиватор за раз; пока база больше порога, фоновая докачка тел приостановлена
* Fri Sep 11 2026 redmail <redmail@example.com> - 0.0.1-72
- Автоархив: письма с заголовками в неизвестной кодировке (unknown-8bit) переносятся, а не пропускаются
- Полная копия: папка Gmail «Вся почта» (зеркало всех писем) не синхронизируется и не удваивает базу
* Fri Sep 11 2026 redmail <redmail@example.com> - 0.0.1-71
- Автоархив без удаления с сервера работает молча в фоне; вопрос с русскими кнопками только при включённом удалении с сервера
* Fri Sep 11 2026 redmail <redmail@example.com> - 0.0.1-70
- Автоархив проверяется после каждых 200 скачанных писем, а не только после полной докачки ящика
* Fri Sep 11 2026 redmail <redmail@example.com> - 0.0.1-69
- Канал управления для голосового помощника: пошаговое заполнение открытой формы встречи (event_form_*), поиск контактов по фамилии на слух (find_contacts)
* Fri Sep 11 2026 redmail <redmail@example.com> - 0.0.1-68
- Автоархив по размеру базы: старые письма переносятся в файлы архива (с сервера НЕ удаляются, если не включить отдельную галочку); архивные письма остаются в списке, тело читается из архива; первый запуск с подтверждением
- Пересылка/открытие письма: тело уже показанного письма не перезагружается, иначе грузится в фоне; окно пересылки открывается сразу, внешние картинки подставляются в него в фоне
* Fri Sep 11 2026 redmail <redmail@example.com> - 0.0.1-67
- Исправлено зависание окна при заполнении списка писем на полной локальной копии
- Хранилище паролей: повторные попытки, запасной файл в профиле по согласию пользователя, адрес D-Bus по умолчанию
- Фоновая загрузка тел писем не забирает процессор у интерфейса
* Fri Sep 11 2026 redmail <redmail@example.com> - 0.0.1-66
- Синхронизация: одна за раз на ящик; открытие несинхронизированной папки не блокирует окно
* Fri Sep 11 2026 redmail <redmail@example.com> - 0.0.1-65
- Хранилище, этап 1: профиль (каталог с базами почты/календаря/контактов, настраивается), полная локальная копия ящика: заголовки всех папок, тела фоном от новых к старым, письма больше 25 МБ по запросу, зеркалирование удалений и флагов с сервера
- Настройки → «Хранилище»: каталог профиля, порог размера письма, порог автоархива, статистика базы
- IMAP: сырые UTF-8 заголовки читаются верно
* Fri Sep 11 2026 redmail <redmail@example.com> - 0.0.1-64
- Подключение ящиков и обновление папок в фоне с индикатором хода; импорт контактов с окном ожидания; адреса контактов в нижнем регистре
- Мини-календарь без рамок, выделение дня акцентом; поля «Кому»/«Тема» выше и с рамкой, кнопки очистки получателей/участников, описание события выше
- Пересылка: внешние картинки письма встраиваются в письмо
- Календарь: подписка на Google Календарь / .ics по ссылке
- Кнопки архивов перенесены на панель дерева папок
* Fri Sep 11 2026 redmail <redmail@example.com> - 0.0.1-63
- Локальный канал управления (unix-сокет redmail-ipc, JSON) для голосового помощника audioreferent: открыть почту, новое письмо/встреча с заполненными полями, перенос/отмена встречи, правила сортировки; отправка только после подтверждения человеком
* Fri Sep 11 2026 redmail <redmail@example.com> - 0.0.1-62
- Журнал подключений и синхронизаций (~/.config/redmail/logs/redmail.log, ротация); Справка → «Журнал подключений…»
- Поля keytab/principal убраны из настроек
* Fri Sep 11 2026 redmail <redmail@example.com> - 0.0.1-61
- CalDAV: библиотека caldav 2.0.x (без AGPL-компонента icalendar-searcher и niquests); обнаружение календарей своим разбором PROPFIND по RFC 4918, проверено e2e на Radicale
- Убрано упоминание ИИ из окна «О программе»
* Fri Sep 11 2026 redmail <redmail@example.com> - 0.0.1-60
- IMAP: команды по очереди (блокировка сессии) и таймаут сокета - зависание на «Загрузка…» при переходе между письмами
- Отметки «прочитано»/«отвечено» уходят на сервер в фоне, не блокируя интерфейс
* Thu Sep 10 2026 redmail <redmail@example.com> - 0.0.1-59
- Два режима списка писем: таблица и плитки (аватар, две строки), переключатель на панели, режим запоминается
- Фильтр списка: только важные, только с вложениями, по маркеру (любой/цвет)
- На письмо можно ставить несколько маркеров (меню с галочками, иконка-сетка в таблице)
* Thu Sep 10 2026 redmail <redmail@example.com> - 0.0.1-58
- CalDAV: HTTP/1.1-сессия (requests) вместо niquests/HTTP/2 - обрыв PUT события; поиск календарей обходит все calendar-home-set и вложенные коллекции (расшаренные)
- Адресная книга: множественный выбор галочками
- IMAP: переподключение при [UNAVAILABLE] после сбоя/простоя сервера
- Ответить всем - в тулбаре и в окне письма
- SSO: keytab и principal как источник билета Kerberos (IMAP/SMTP/CalDAV)
- Заставка показывается сразу (импорт интерфейса отложен, версия дорисовывается после)
- Маркеры не пропадают: \Flagged без keyword = маркер, цвет из локального кэша
* Thu Sep 10 2026 redmail <redmail@example.com> - 0.0.1-57
- CalDAV: вход по Kerberos (SPNEGO/Negotiate) тем же доменным билетом, что IMAP/SMTP - без app-пароля
* Wed Sep 09 2026 redmail <redmail@example.com> - 0.0.1-56
- SMTP: письма с кириллицей больше не уходят как сырой 8bit без BODY=8BITMIME (причина 500 Message rejected на строгих серверах именно из redmail)
* Wed Sep 09 2026 redmail <redmail@example.com> - 0.0.1-55
- Пересылка письма больше не теряет картинки (передаётся HTML + встроенные изображения)
- IMAP: восстановление сессии после долгого простоя (illegal in state NONAUTH)
- Сообщения об ошибках больше не показывают сырые байты (b...)
- CalDAV: проверка подключения не обрывается целиком из-за сбоя списка календарей
- CalDAV: диалог смены адреса календаря — нормальная ширина поля + поиск на сервере
- Заставка при запуске гарантированно отрисовывается перед загрузкой интерфейса
* Tue Sep 09 2026 redmail <redmail@example.com> - 0.0.1-54
- CalDAV: обнаружение расшаренных календарей на сервере (VK Mail и др.) при добавлении календаря
- CalDAV: исправлен баг — второй настроенный календарь того же аккаунта синхронизировался с тем же самым первым календарём
* Tue Sep 08 2026 redmail <redmail@example.com> - 0.0.1-53
- Написать/Ответить/Переслать/Удалить теперь с подписью рядом с иконкой (по референсу)
* Tue Sep 08 2026 redmail <redmail@example.com> - 0.0.1-52
- Иконки тулбаров и папок ярче в тёмной теме (лучше контраст)
* Tue Sep 08 2026 redmail <redmail@example.com> - 0.0.1-51
- Иконки тулбаров и папок заменены на Material Symbols (тот же стиль, что на референсе дизайна)
* Tue Sep 08 2026 redmail <redmail@example.com> - 0.0.1-50
- Дерево папок: своя иконка для каждой роли папки (входящие/отправленные/черновики/корзина/спам/важное/помеченные/вся почта)
- Дерево папок: число непрочитанных писем рядом с названием папки
* Tue Sep 08 2026 redmail <redmail@example.com> - 0.0.1-49
- Дизайн: кнопки-иконки тулбаров получили рамку и больший зазор между собой
- Дизайн: поле поиска/фильтра оформлено как скруглённая таблетка со значком лупы
- Дизайн: скруглённое выделение текущей папки в дереве папок
* Mon Sep 07 2026 redmail <redmail@example.com> - 0.0.1-48
- Доступность: контраст приглушённого текста поднят до WCAG AA (плейсхолдеры, дни вне месяца в календаре)
- Дизайн: главное действие диалога (Отправить, Сохранить) теперь выделено акцентной кнопкой
* Mon Sep 07 2026 redmail <redmail@example.com> - 0.0.1-47
- Заголовок письма (Тема/От/Кому/Дата) больше не растягивается на 50-100 процентов поля просмотра, занимает ровно свою высоту
- Список "и ещё N..." получателей теперь показывается всплывающей подсказкой при наведении, а не только по клику
* Mon Sep 07 2026 redmail <redmail@example.com> - 0.0.1-46
- Масштаб шрифта: размер теперь явно применяется ко всем открытым виджетам окна, а не только к новым
* Mon Sep 07 2026 redmail <redmail@example.com> - 0.0.1-45
- Масштаб шрифта (ползунок в статус-баре) теперь применяется и к тексту письма в области просмотра
* Mon Sep 07 2026 redmail <redmail@example.com> - 0.0.1-44
- Редактор подписи: вставка изображений и выбор гарнитуры/размера шрифта, как в окне письма
- Редактор подписи: узкое поле ввода на 4 строки вместо растянутого на весь диалог
* Sun Sep 07 2026 redmail <redmail@example.com> - 0.0.1-43
- CalDAV: нейтральный User-Agent вместо приметного python-caldav
- CalDAV: проверка подключения теперь реально пишет и удаляет тестовое событие, а не только читает список календарей
* Mon Sep 07 2026 RedMail dev <noreply@example.com> - 0.0.1-42
- Длинный список адресатов в заголовке письма теперь сокращается до первых
  3 с кликабельной ссылкой "и ещё N", а не растягивает заголовок на всё окно
- При переключении между папками панель чтения теперь всегда открывает
  реально выбранное письмо, а не оставляет старое/пустое содержимое при
  визуально "выделенной" строке
- В папке "Отправленные" колонка показывает "Кому", а не бесполезное
  "От кого" (это всегда вы сами)
- Письмо, на которое отправлен ответ, теперь помечается значком - обычный
  IMAP-флаг \Answered
- Папка "Отправленные" обновляется сразу после отправки письма, без
  ручного нажатия "Обновить"
- Синхронизация CalDAV теперь повторяет запрос один раз при обрыве
  соединения ("Remote end closed connection without response") вместо
  немедленной ошибки - частая причина, почему проверка подключения
  проходила, а сама синхронизация - нет
- Отклонённые приглашения на встречи больше не отображаются в календаре;
  при повторном открытии уже отвеченного события нужная кнопка ответа
  теперь неактивна
- Письмо, которое не удалось декодировать текстом, показывает понятную
  подпись вместо молча пустой панели чтения
- Пересланные письма целиком (как вложение .eml) теперь корректно
  показываются как вложение и в списке, и при открытии письма - раньше
  такое вложение вообще терялось, а его текст примешивался к телу письма

* Mon Sep 07 2026 RedMail dev <noreply@example.com> - 0.0.1-41
- Настройка источника календаря перенесена из Параметров в сам календарь -
  кнопка "+ Добавить календарь" теперь открывает выбор источника
  (Локальный / CalDAV / Google - честно отключён, пока не поддерживается)
  вместо простого запроса названия
- Можно подключить НЕСКОЛЬКО внешних CalDAV-календарей одновременно с
  локальными - раньше был только один общий адрес CalDAV на весь аккаунт
- Синхронизация с CalDAV теперь помечает полученные события правильным
  календарём вместо того, чтобы всегда класть их в календарь по умолчанию
- Ранее настроенный общий адрес CalDAV автоматически переносится в
  отдельный календарь при первом запуске этой версии

* Mon Sep 07 2026 RedMail dev <noreply@example.com> - 0.0.1-40
- Положение разделителей окна (ширина дерева папок, доля списка писем и
  панели чтения) теперь сохраняется между запусками - раньше сбрасывалось
  каждый раз на жёсткое умолчание, из-за чего список писем мог остаться
  узкой полоской, а колонка "Дата" (с уже закреплённой шириной) выдавливала
  остальные
- Разделитель между списком писем и панелью чтения по умолчанию делит
  место в пропорции 2:1, а не строго пополам
- Рамка чекбокса отметки письма в тёмной теме стала чуть ярче

* Sun Sep 06 2026 RedMail dev <noreply@example.com> - 0.0.1-39
- Убрана лишняя "рамка внутри карточки события" в календаре - карточка
  событий теперь рисуется как единая скруглённая форма с полоской цвета
  календаря слева, без артефакта из двух наложенных скруглённых фигур

* Sun Sep 06 2026 RedMail dev <noreply@example.com> - 0.0.1-38
- Событие в другом календаре теперь красится цветом ЭТОГО календаря, а не
  всегда одним и тем же цветом "я организатор" - раньше событие сохранялось
  в нужный календарь корректно, но выглядело неотличимо от календаря по
  умолчанию, поэтому казалось, что оно "не попадает" в новый календарь
- Ручное изменение ширины колонки "Дата" в списке писем теперь действительно
  сохраняется между запусками программы - раньше следующий же запуск (и
  иногда даже это же самое открытие окна) тихо возвращал её обратно
- Рамка чекбокса отметки письма стала нейтрально-серой в обеих темах

* Sun Sep 06 2026 RedMail dev <noreply@example.com> - 0.0.1-37
- Внешние картинки в HTML-письмах снова показываются - Chromium по
  умолчанию не даёт странице, открытой с диска, качать что-либо по сети,
  из-за чего они молча не грузились
- Рамка чекбокса отметки письма в списке стала ещё тоньше
- Клик по ещё не наступившему сегодня часу в сетке календаря (например,
  утро, если сейчас день) больше не отклоняется молча как "прошедшее
  время" - раньше это выглядело как "событие не создаётся"

* Sun Sep 06 2026 RedMail dev <noreply@example.com> - 0.0.1-36
- HTML-письма теперь отрисовываются настоящим браузерным движком
  (Chromium через QWebEngineView) вместо упрощённого текстового виджета
  Qt - раньше сложные маркетинговые письма с картинками-подложками и
  блоками рядом отображались криво (текст поверх картинки внахлёст).
  Просмотр писем по-прежнему защищён: JavaScript, локальное хранилище и
  cookie в письмах полностью отключены, клик по ссылке открывает
  системный браузер, а не саму программу

* Sun Sep 06 2026 RedMail dev <noreply@example.com> - 0.0.1-35
- Список писем: при ручном сужении колонки "Тема" мышью справа больше не
  остаётся пустоты - освободившееся место теперь всегда забирает "Дата"
- Рамка чекбокса отметки письма в тёмной теме стала вдвое тоньше
- При открытии календаря в виде "Неделя" (по умолчанию) мини-календарь
  слева теперь тоже сразу показывает сегодняшнее число, а не понедельник
  текущей недели
- Кнопка "Новая встреча" на панели инструментов могла молча не создать
  событие, если открыть её после 9 утра - исправлено
- Внешние картинки в HTML-письмах теперь кэшируются в памяти на время
  работы программы - повторное открытие уже прочитанного письма с такими
  картинками больше не скачивает их заново по сети

* Sun Sep 06 2026 RedMail dev <noreply@example.com> - 0.0.1-34
- Кэш писем теперь сохраняет HTML и встроенные картинки - раньше при
  повторном открытии письма кэш всегда показывал голый текст без
  оформления и картинок, даже если сервер отдавал полный HTML
- В редакторе письма заработала вставка картинки из буфера обмена
- При открытии календаря теперь сразу выделено сегодняшнее число
- Новая встреча по умолчанию создаётся в календаре, выбранном в списке
  "Мои календари" слева, а не всегда в календаре по умолчанию
- Импорт .ics теперь создаёт для него отдельный именованный календарь,
  а не сваливает события в календарь по умолчанию
- В тёмной теме чекбокс отметки письма стал виден одинаково хорошо и в
  выделенной, и в обычной строке списка писем

* Sun Sep 06 2026 RedMail dev <noreply@example.com> - 0.0.1-33
- Прокрутка к письму в цепочке пересланных/связанных писем теперь
  срабатывает надёжно - раньше могла показать более старое письмо
  цепочки вместо только что открытого, выглядело как ошибка
  отображения письма
- Столбцы Дата и Тема в списке писем снова можно менять шириной
  вручную - предыдущий фикс растягивания таблицы случайно это
  сломал
- В тёмной теме теперь видно чекбоксы отметки писем для удаления
- При написании письма можно вставлять картинки прямо в текст и
  выделять текст полужирным, курсивом, подчёркиванием, менять
  шрифт и размер
- Добавлены подписи для писем - одна или несколько, с выбором
  нужной прямо при написании письма, раздел Параметры - Подписи

* Sun Sep 06 2026 RedMail dev <noreply@example.com> - 0.0.1-32
- Внешние картинки в письмах теперь грузятся параллельно, а не по
  одной - в письме с 5-6 картинками (логотип, баннер, иконки) это
  раньше могло выглядеть так, будто загрузка вообще не сработала,
  если не подождать достаточно долго
- Если пользователь открыл другое письмо, пока грузились картинки
  предыдущего, устаревший результат больше не может неожиданно
  подменить то, что сейчас показано на экране

* Sun Sep 06 2026 RedMail dev <noreply@example.com> - 0.0.1-31
- Открытие письма (особенно ещё не читанного - первая загрузка
  с сервера) больше не подвешивает всю программу целиком - раньше
  на медленной сети зависало окно и переставало реагировать на
  клики мыши вообще. Теперь письмо грузится в фоне
- Внешние картинки в HTML-письмах теперь подгружаются сами -
  раньше не показывались вовсе (жалоба: картинки теряются)
- Окно О программе и заставка при запуске показывают настоящую
  версию установленного пакета (например 0.0.1-31), а не всегда
  одну и ту же 0.0.1

* Sun Sep 06 2026 RedMail dev <noreply@example.com> - 0.0.1-30
- Список писем: исправлено повторное появление бага - таблица не
  растягивается на всё окно. Сохранённая раскладка колонок у уже
  установленных экземпляров перетирала фикс из 0.0.1-28 сразу же
  после запуска

* Sat Sep 05 2026 RedMail dev <noreply@example.com> - 0.0.1-29
- Устранено сильное замедление интерфейса, появившееся в 0.0.1-28
  из-за темы оформления - жалоба: письма читаются медленно,
  переключение между окнами тоже. Причина - слишком широкий стиль
  на все виджеты сразу; тема переведена на палитру Qt (быстрый
  штатный механизм) без потери светлой/тёмной темы

* Sat Sep 05 2026 RedMail dev <noreply@example.com> - 0.0.1-28
- Список писем теперь всегда растягивается на всю ширину панели
  (не оставляет пустую полосу справа), особенно заметно было при
  чтении справа от списка
- Календарь: теперь можно завести несколько своих календарей
  (добавить/переименовать/перекрасить/удалить), у каждого своя
  видимость и цвет; событие привязывается к выбранному календарю
- Календарь: нельзя больше запланировать или перенести встречу
  на прошедшую дату/время - показывается предупреждение
- Календарь: карточка события в сетке теперь залита выбранным
  цветом (втрое светлее), а не просто белая с цветной полоской
- Действия календаря и контактов переведены с текстовых кнопок
  на иконки с подсказками, как раньше сделано для писем/архива
- Параметры и Справка перенесены в общий ряд кнопок рядом с
  Почта/Календарь/Контакты
- Кнопки Открыть архив, Импортировать, Архивировать папку
  перенесены к кнопкам над списком писем; Обновить тоже
  перенесена туда и поставлена первой
- Добавлена своя тема оформления (светлая/тёмная) в Параметрах -
  не зависит от темы рабочего стола
- При запуске теперь показывается заставка с названием, автором
  и ходом загрузки программы, до открытия основного окна

* Fri Sep 04 2026 RedMail dev <noreply@example.com> - 0.0.1-27
- Отправка почты по SMTP (в т.ч. рассылка приглашений на встречу):
  единичный обрыв соединения с сервером посреди разговора
  ("Connection unexpectedly closed") теперь тоже приводит к
  повторной попытке на свежем соединении, а не сразу к ошибке -
  жалоба: "Встреча сохранена, но не разослана"

* Fri Sep 04 2026 RedMail dev <noreply@example.com> - 0.0.1-26
- Адресная книга: при выборе 2-го и последующих адресатов из списка
  теперь корректно подставляется разделитель и распознаётся именно
  введённый префикс, а не остаток предыдущего адреса
- Адресная книга: обрывок недописанного вручную текста больше не
  попадает в список получателей как отдельный (битый) адрес
- Синхронизация/чтение писем: разрыв TLS-соединения посреди сессии
  ("socket error: EOF ... _ssl.c:2437") теперь тоже приводит к
  переподключению и повтору операции, а не к ошибке в интерфейсе
- Правила сортировки почты теперь можно редактировать (двойной клик
  по правилу или кнопка «Изменить…»), а не только
  удалять и создавать заново
- Применение правил сортировки: если папка-назначение ещё не создана
  на сервере, она создаётся автоматически вместо ошибки
  "[TRYCREATE] Folder does not exist"

* Thu Sep 03 2026 RedMail dev <noreply@example.com> - 0.0.1-25
- SSO (Kerberos): точнее диагностирует случай, когда сервер заявляет
  поддержку GSSAPI (в CAPABILITY/EHLO), но реально не может завершить
  обмен — теперь сообщение прямо говорит, что дело в неполной/сломанной
  настройке GSSAPI на сервере, а не в билете Kerberos, вместо
  непонятной протокольной ошибки ("unexpected response" и т.п.)

* Thu Sep 03 2026 RedMail dev <noreply@example.com> - 0.0.1-24
- Исправлена подстановка адресата из адресной книги — лишний
  набранный текст больше не прилипает к вставленному адресу после
  выбора из выпадающего списка
- В диалоге создания/редактирования встречи вложения теперь можно
  открыть двойным кликом или сохранить через правую кнопку мыши — до
  сих пор это работало только при просмотре чужого приглашения

* Wed Sep 02 2026 RedMail dev <noreply@example.com> - 0.0.1-23
- CalDAV: при ошибке входа теперь проверяется, какие схемы
  аутентификации реально предлагает сервер (заголовок
  WWW-Authenticate) — если сервер поддерживает Negotiate
  (Kerberos/SPNEGO), сообщение об этом прямо скажет, что CalDAV в
  приложении пока умеет только логин и пароль, а не молча покажет
  "Unauthorized"

* Wed Sep 02 2026 RedMail dev <noreply@example.com> - 0.0.1-22
- SSO (Kerberos) для IMAP/SMTP теперь заранее проверяет, объявляет ли
  сервер поддержку GSSAPI (по CAPABILITY/EHLO), и если нет — показывает
  понятное сообщение с перечнем реально поддерживаемых способов входа,
  вместо непонятной ошибки протокола "BAD invalid command"/"Invalid
  command". Найдено при первой проверке на реальном сервере VK Mail.

* Tue Sep 01 2026 RedMail dev <noreply@example.com> - 0.0.1-21
- Исправлен импорт .vcf: если у карточки пустые FN и N, но ФИО лежит в
  NICKNAME (нестандартно, но встречается у реальных экспортёров) —
  теперь оно подхватывается, а не теряется в пользу email в качестве
  имени

* Tue Sep 01 2026 RedMail dev <noreply@example.com> - 0.0.1-20
- В «Параметры…» добавлена кнопка «Проверить подключение» отдельно
  для входящей почты (IMAP), исходящей (SMTP) и календаря (CalDAV) —
  проверяет подключение и вход (включая SSO), ничего не отправляя и
  не блокируя окно

* Tue Aug 25 2026 RedMail dev <noreply@example.com> - 0.0.1-19
- Исправлена рамка карточки события в календаре (в прошлой сборке
  убралась не та рамка, оставив только цветную полосу слева) — теперь
  внешняя рамка на месте, а лишняя внутренняя рамка вокруг текста
  описания встречи (в диалоге события) действительно убрана
- Исправлено зависание при просмотре цепочки писем — загрузка
  остальных писем цепочки теперь идёт в фоне, письмо открывается
  сразу

* Mon Aug 24 2026 RedMail dev <noreply@example.com> - 0.0.1-18
- Просмотр письма из цепочки (вопрос-ответ) теперь показывает всю
  цепочку подряд, одной прокручиваемой лентой, а не просто список
  ссылок на другие письма
- Импорт .pst: архив теперь создаётся автоматически, спрашивается
  только название; каталог для новых архивов задаётся один раз в
  «Параметры…»
- Импорт .pst: убраны служебные уровни «(без имени)/Top of Personal
  Folders» — сразу показываются реальные папки; папки «Календарь»/
  «Контакты» больше не превращаются в мусорные «письма»
- Исправлено переименование папок в архиве (не работало на
  промежуточных узлах дерева)
- Добавлено перетаскивание папок мышью — и в архивах, и в живых
  ящиках
- Большинство команд на панелях инструментов переведены на иконки с
  подсказками (кроме Почты/Календаря/Контактов/Параметров/Справки) —
  экономит место; команды над письмами (Написать/Ответить/Переслать/
  Удалить/В архив) перенесены в среднюю панель, над строкой фильтра
- Убрана лишняя внутренняя рамка у карточек событий календаря
- Добавлен вход через SSO (Kerberos) для обычных учётных записей
  IMAP/SMTP (почтовый сервер в домене) — как уже было для Exchange;
  логин/пароль остаются доступны как раньше
- Исправлена уязвимость: имя файла вложения могло содержать путь для
  выхода за пределы каталога при открытии/сохранении на диск

* Mon Aug 24 2026 RedMail dev <noreply@example.com> - 0.0.1-17
- Добавлен вход через SSO (Kerberos) для обычных учётных записей
  IMAP/SMTP (почтовый сервер в домене) — как уже было для Exchange
  (EWS). В параметрах учётной записи выбирается способ входа «Логин и
  пароль» (по умолчанию, как раньше) или «SSO (Kerberos, без пароля)»:
  во втором случае пароль в приложении не запрашивается и не хранится
  — используется Kerberos-билет, который RED OS уже выдала при входе
  пользователя в домен (SSSD); смену пароля на стороне домена
  отслеживает сама SSO-инфраструктура, приложению обновлять ничего не
  нужно.

* Mon Aug 24 2026 RedMail dev <noreply@example.com> - 0.0.1-16
- Исправлена уязвимость: имя файла вложения (из письма или .ics-приглашения)
  подставлялось напрямую в путь при открытии/сохранении на диск —
  вложение с именем вида «../../.ssh/authorized_keys» или абсолютным
  путём могло записать данные вне временного каталога. Теперь
  используется только базовое имя файла.

* Mon Aug 24 2026 RedMail dev <noreply@example.com> - 0.0.1-15
- Из черновика теперь можно отправить письмо: определение папок
  «Черновики»/«Отправленные»/«Корзина» больше не полагается только на
  флаг SPECIAL-USE (его отдаёт не каждый сервер) — есть запасной
  поиск по имени папки
- Добавлена «Очистить корзину…» по правой кнопке на папке корзины
- В отдельном окне письма появились кнопки «Ответить»/«Переслать»;
  эти кнопки, как и «Удалить»/«В архив…», больше не активны на
  страницах Контактов и Календаря
- При просмотре письма показывается список других писем той же цепочки
  (тема без Re:/Fwd:/Ответ:/Пересыл:) с переходом по клику
- Исправлено: письма в HTML без явного Content-Disposition (например,
  рекламные рассылки вроде Авито) ошибочно попадали во вложения вместо
  тела письма
- Исправлено: одно повреждённое сообщение в .pst больше не прерывает
  весь импорт целиком — оно пропускается, остальные загружаются
- Дерево папок архива при импорте .pst теперь строится как настоящее
  вложенное дерево, а не список папок с полным путём в одну строку

* Mon Aug 24 2026 RedMail dev <noreply@example.com> - 0.0.1-14
- Копия отправленного письма теперь сохраняется в «Отправленные» для
  IMAP-аккаунтов (сервер не всегда делает это сам); появилась кнопка
  «Сохранить черновик», черновики можно открыть на редактирование
  двойным кликом
- Из «Корзины» теперь можно удалить/восстановить сразу несколько писем
  (выделение как в проводнике, не только галочками); добавлено
  «Восстановить из корзины»
- При просмотре письма показываются тема/отправитель/получатели; письмо
  можно открыть в отдельном окне
- Контакты: столбцы таблицы растягиваются пропорционально; добавлено
  «Удалить все…»; учётную запись живой почты можно отключить (правая
  кнопка мыши в дереве папок)
- Исправлено: имя контакта с запятой (формат «Фамилия, Имя») ломало
  разбор списка адресов в поле «Кому» — второй адресат переставал
  распознаваться
- Синхронизация CalDAV больше не подвешивает всё приложение на время
  сетевого запроса — выполняется в фоновом потоке, у HTTP-запроса
  появился таймаут
- IMAP-сессия сама переподключается при обрыве связи после простоя,
  вместо того чтобы показывать сырую сетевую ошибку

* Mon Aug 24 2026 RedMail dev <noreply@example.com> - 0.0.1-13
- Добавлено окно «О программе» (Справка → О программе…) — автор и
  упоминание о разработке с использованием ИИ
- Добавлено прямое подключение к Exchange по протоколу EWS (Параметры →
  «Подключить Exchange (EWS)…») — для серверов, где IMAP отключён
  политикой безопасности; поддерживает вход по логину/паролю, NTLM и
  Kerberos (единый вход, SSO, без ввода пароля в приложении). Работает
  как ещё одна учётная запись наравне с IMAP — та же таблица писем,
  дерево папок, архивирование, календарь; письмо через EWS-аккаунт
  отправляется через сам Exchange, отдельный SMTP не нужен

* Sun Aug 23 2026 RedMail dev <noreply@example.com> - 0.0.1-12
- Импорт .pst/.mbox/Maildir и отправка приглашений/ответов по календарю
  (создание, перенос мышью, отмена встречи, ответ на приглашение) больше
  не блокируют интерфейс — выполняются в фоновом потоке; у импорта
  индикатор процесса, у отправки — сообщение в строке состояния
- Если ещё не открыто ни одного архива, диалог выбора архива больше не
  показывает бесполезный единственный пункт — сразу открывается диалог
  создания нового архива
- Открытые/импортированные архивы теперь можно отключить: правая кнопка
  мыши на архиве в дереве папок → «Закрыть архив» (сам файл не удаляется)
- Исправлено декодирование имени отправителя в архиве для писем, где в
  From нет реального адреса в <угловых скобках> — раньше в таких письмах
  имя отправителя оставалось нерасшифрованным (=?utf-8?b?...?=)
- Папки теперь можно переименовать (правая кнопка мыши → «Переименовать
  папку…») — и созданные на сервере в живом ящике, и папки внутри архива

* Sun Aug 23 2026 RedMail dev <noreply@example.com> - 0.0.1-11
- Панель инструментов почты разбита на два ряда — раньше при большом
  числе кнопок Qt прятал "лишние" (в т.ч. Параметры) за скрытую стрелку
- Правила сортировки почты и добавление учётной записи перенесены в
  Параметры (были отдельными кнопками на переполненной панели)
- Событие в виде месяца (таблетка) больше не пытается перетаскиваться
  мышью — перенос там раньше не работал (не был реализован), теперь
  явно недоступен вместо обманчивого полу-перетаскивания

* Thu Aug 20 2026 RedMail dev <noreply@example.com> - 0.0.1-2
- Календарь: недельная сетка (даты в кружках, линия "сейчас", drag-перенос
  с рассылкой участникам), вложения к событиям, кликабельные ссылки
- Адресная книга: локальное хранилище, импорт vCard/CSV, автодополнение
  в письмах и встречах, "добавить отправителя в контакты"
- Фильтр писем по цвету маркера; открытые архивы переживают перезапуск

* Wed Aug 19 2026 RedMail dev <noreply@example.com> - 0.0.1-1
- Первая сборка: почта (IMAP/SMTP, кэш, архивы), календарь (события, iTIP-приглашения/ответы/переносы, повторы)
